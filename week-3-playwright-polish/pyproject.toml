[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--browser chromium --browser firefox --browser webkit -n 4"
base_url = "https://www.saucedemo.com"

markers = [
    "smoke: quick baseline checks",
    "regression: full test suite",
]